import 'package:flutter/material.dart';

class OneItemView extends StatelessWidget {
  const OneItemView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text("One Item View"),
      );

  }
}
